{
    "private": true,
    "scripts": {
        "build:wasm": "for crate in packages/wasm-*; do wasm-pack build \"$crate\" --target bundler || exit 1; done"
    },
    "dependencies": {
        "katex": "^0.17.0"
    }
}